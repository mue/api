meta {
  name: Get Static Map
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v2/map
  body: none
  auth: none
}

params:query {
  latitude: 51.5074
  longitude: -0.1278
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("missing coords returns 400", function() {
    // Run separately: GET /v2/map (no params) → 400
  });
}
