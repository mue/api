meta {
  name: Geocode Missing q Param
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v2/geocode
  body: none
  auth: none
}

tests {
  test("status is 400", function() {
    expect(res.status).to.equal(400);
  });

  test("returns error message", function() {
    expect(res.body).to.have.property("error");
  });
}
