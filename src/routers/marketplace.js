module.exports = ({ server, config }) => {
    server.get('/marketplace/featured', async () => {

    });

    server.get('/marketplace/all', async () => {

    });

    server.get('/marketplace/item/:name', async () => {

    });
};